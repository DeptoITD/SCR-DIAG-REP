# Configuración Servidores
# =========================

# Rutas (agnósticas, funcionan cualquier máquina)
REPO_PATH="/opt/scripts/SCR-DIAG-REP"
EXPORT_PATH="${REPO_PATH}/src/export"
LOG_DIR="${REPO_PATH}/logs"

# Samba
SAMBA_PASSWORD="sambapass123"  # Cambiar para producción

# --- OPCIONAL (solo para documentación/auditoría) ---
# SERVIDOR_ORIGEN_HOSTNAME="srv2"   # Nombre servidor origen (para referencia)
# SERVIDOR_ORIGEN_IP="192.168.1.16" # IP servidor (para auditoría logs)
# NAS_IP="192.168.1.19"             # IP NAS (para auditoría logs)
